name: Legacy static deployment disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "This compatibility workflow is disabled. Deployment is handled by deploy-pages.yml."
