# QA v4.2 — LuQvia Website Production Integration

Run:

```bash
npm run check
```

Release-specific checks are written to:

- `reports/v4.2-luqvia-website-production-qa.json`

The QA verifies the central configuration, all 20 source articles, generated panels, operator relationship disclosure, pricing text, three conversion routes, source attribution, non-affiliate treatment, table-of-contents links, Service structured data, exclusion from unrelated comparisons, and zero-error site validation/content audit.
